"use client";
import {useState} from "react"; import {ArrowRight,Check} from "lucide-react";
export default function Newsletter(){const [email,setEmail]=useState(""); const [done,setDone]=useState(false); const submit=(e:React.FormEvent)=>{e.preventDefault(); if(email.trim()) setDone(true)}; return <section className="mx-auto max-w-7xl px-6 py-20"><div className="overflow-hidden rounded-[2rem] bg-zinc-950 p-8 text-white md:p-12"><div className="max-w-2xl"><p className="text-sm font-bold uppercase tracking-widest text-blue-400">Newsletter</p><h2 className="mt-3 text-3xl font-black md:text-4xl">Ideas worth reading. Occasionally.</h2><p className="mt-4 leading-7 text-zinc-400">Get new articles about data, web development, AI and my learning journey.</p>{done?<div className="mt-7 flex items-center gap-2 font-semibold"><Check className="text-green-400"/> You’re on the list.</div>:<form onSubmit={submit} className="mt-7 flex flex-col gap-3 sm:flex-row"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="min-w-0 flex-1 rounded-full bg-white px-5 py-3 text-zinc-900 outline-none"/><button className="inline-flex items-center justify-center gap-2 rounded-full bg-blue-600 px-6 py-3 font-bold hover:bg-blue-500">Subscribe <ArrowRight size={17}/></button></form>}</div></div></section>}
